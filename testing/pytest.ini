[pytest]
testpaths = .
python_files = test_*.py
addopts = -q --strict-markers
markers =
    live: needs network access to the camera grid (slow)
    slow: takes more than a few seconds
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
